import * as React from "react";
export default class Sitemaps extends React.Component {
  constructor(props: any) {
    super(props);
  }
  state = {
    expand: true,
  };

  render() {
    return <div>{"sitemaps"}</div>;
  }
}
